@page
@model SupermarketWEB.Pages.PayMode.IndexModel
@{
    ViewData["Title"] = "Index PayMode";
}

<h1>Index PayMode</h1>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PayMod[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PayMod[0].Description)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.PayMod)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <span></span>
                </td>
            </tr>
        }
    </tbody>
</table>
